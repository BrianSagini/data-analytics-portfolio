.env
*.env
!.env.example
.venv/
venv/
__pycache__/
*.pyc
*.log
secrets/
credentials/
airflow/logs/*
!airflow/logs/.gitkeep
projects/*/data_raw/*
!projects/*/data_raw/.gitkeep
.DS_Store
*.egg-info/
.pytest_cache/

# Power BI: .pbip projects (TMDL/PBIR) are text and git-friendly and ARE
# committed; these are per-machine local cache/credential artifacts Power BI
# Desktop generates next to them and must never be committed.
**/.pbi/
**/*.pbix
**/CachedData/
**/*.abf

# Standalone repos relocated here each carry their own .git and their own
# GitHub remote -- this repo's git must not track them as regular files.
standalone-repos/
